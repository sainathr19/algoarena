import { Card, CardFooter, CardHeader, CardTitle } from "@/components/ui/card";
import React from "react";

const ContestPreview = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Algo Arena Weekly</CardTitle>
      </CardHeader>
      <CardFooter>{``}</CardFooter>
    </Card>
  );
};

export default ContestPreview;
